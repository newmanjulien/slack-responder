# Slack Responder

Responder Slack app (App B). Posts inbound relay messages into per-user channels and relays replies back to App A.

## Local dev
- Copy `.env.example` to `.env` and fill values.
- `npm install`
- `npm run dev`
