# Shared code

This folder is synced from slack-overbase/shared.

- `shared/relay/types.ts` defines the relay envelope + file metadata types.
- `shared/convex-api` contains generated Convex API types.
